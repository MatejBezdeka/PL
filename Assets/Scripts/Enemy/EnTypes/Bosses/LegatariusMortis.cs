using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LegatariusMortis : Enemy
{
    //mesenger of death
    //
    //Mensis Martius
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public LegatariusMortis(GameObject prefab, Vector3 spawnPoint) : base(prefab, spawnPoint) {
    }
}
